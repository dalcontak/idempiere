# =============================================================================
# iDempiere Secret - Sensitive configuration
# =============================================================================
# IMPORTANT: Replace base64 values before applying!
# Generate values with: echo -n "your-password" | base64
#
# For production, use sealed-secrets or external-secrets-operator instead.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: idempiere-secret
  namespace: idempiere
  labels:
    app.kubernetes.io/name: idempiere
    app.kubernetes.io/component: server
type: Opaque
data:
  # echo -n "adempiere" | base64
  DB_PASS: YWRlbXBpZXJl
  # echo -n "postgres" | base64
  DB_ADMIN_PASS: cG9zdGdyZXM=
  # echo -n "myPassword" | base64
  KEY_STORE_PASS: bXlQYXNzd29yZA==
  # echo -n "info" | base64
  MAIL_PASS: aW5mbw==
